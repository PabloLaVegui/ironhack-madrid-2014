<h2>Challenge list</h2>

<table>
  <tr>
    <th>Challenge name</th>
    <th>Date of creation</th>
  </tr>

  <% @challenges.each do |ch| %>
    <tr>
      <td><%= link_to ch.name, challenge_path(ch) %></td>
      <td><%= ch.created_at %></td>
    </tr>
  <% end %>

</table>
